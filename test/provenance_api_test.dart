import 'package:azure_devops_sdk/api.dart';
import 'package:test/test.dart';


/// tests for ProvenanceApi
void main() {
  var instance = ProvenanceApi();

  group('tests for ProvenanceApi', () {
    // Creates a session, a wrapper around a feed that can store additional metadata on the packages published to it.
    //
    //Future<SessionResponse> createSession(String organization, String protocol, String project, String apiVersion, SessionRequest body) async 
    test('test createSession', () async {
      // TODO
    });

  });
}
